#include<stdio.h>
int main()
{
char ch;
printf("enter the alphabet:",ch);
  if(ch>='a' && ch<='z' ||ch='A' && ch='Z')
  {
 printf("it is a alphabet",ch);
  }
  else
  {
    printf("it is not a alphabet",ch);
  }
  return 0;
}
